import {Text, View} from "react-native";

function SubmitTaskScreen({ navigation }) {
    return (
        <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
            <Text>SubmitTaskScreen!</Text>
        </View>
    );
}

export default SubmitTaskScreen;